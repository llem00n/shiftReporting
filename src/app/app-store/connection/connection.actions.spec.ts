import * as fromConnection from './connection.actions';

// describe('setConnections', () => {
//   it('should return an action', () => {
//     expect(fromConnection.setConnections().type).toBe('[Connection] Set Connections');
//   });
// });
